# library (Compatibility Alias)

Use `Library/README.md` for the full reference library.

This lowercase folder is included for consistency with repository family patterns and scripts that expect `library/`.

Single-file guide:
- `../Library/00_full_course_master_guide.md`
